# GitHub Metadata

## Suggested repository description

Capability-based LLM routing for application teams, with free/paid tracks, fallback policy, and pluggable gateway adapters.

## Suggested topics

```text
llm
ai-infra
routing
policy-router
portkey
litellm
openai-compatible
ai-gateway
self-hosted
nodejs
```

## Suggested website / homepage

Use the repository URL itself unless a dedicated docs site exists.

## Suggested social preview headline

One reusable LLM routing layer for multiple apps, providers, and gateways.

## Suggested pinned highlights

- capability-based routing instead of model-specific business code
- free-first and paid-fallback workflows
- Portkey and LiteLLM gateway adapters
- CLI for `init`, `doctor`, and `simulate`
- local Portkey-compatible dev gateway for live validation
