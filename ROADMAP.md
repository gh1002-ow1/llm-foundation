# Roadmap

## Post-1.0 near term

- split router execution into typed chat and embedding helpers
- add metrics and route event hooks
- expand the CLI into a more guided config workflow
- add publish metadata and public repo URLs for workspace packages
- add a LiteLLM real smoke path

## Mid term

- add budget and policy constraints
- add structured output helpers
- add provider capability matrix
- add retries and timeouts at the router layer
- add route traces and local metrics storage

## Later

- add Python client package
- add remote config sources
- add Web UI for route simulation
- add starter templates for multiple app styles
